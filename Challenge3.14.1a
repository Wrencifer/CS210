#include <iostream>
#include <string>
using namespace std;

int main() {
   string userStr;
	char newChar;

   getline(cin, userStr);
	cin >> newChar;
   
   userStr.at(2) = newChar;

   cout << userStr << endl;

   return 0;
}
