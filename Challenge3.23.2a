#include <iostream>
using namespace std;
 
int main() {
   int in;
   
   cin >> in;
   
   while (in != 10) {
      cout << "Integer is " << in << endl;
      cin >> in;
   }
   
   cout << "Done" << endl;
   
   return 0;
}
